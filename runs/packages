#!/bin/bash

if [[ "$OSTYPE" == "darwin"* ]]; then
  brew install neovim tmux lua luajit luarocks fzf yazi
elif [[ "$OSTYPE" == "linux-gnu"* ]]; then
  sudo apt-get install neovim tmux lua luajit luarocks fzf
fi

